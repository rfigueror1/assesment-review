DROP DATABASE  IF EXISTS tweetDb;

CREATE DATABASE IF NOT EXISTS tweetDb;

USE tweetDb;

CREATE TABLE IF NOT EXISTS tweets (id INT AUTO_INCREMENT, user_data VARCHAR(100), tweetText VARCHAR(100), PRIMARY KEY(id));
